{
  "name": "prompt-proof-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AWS SAM backend for prompt-proof — AI proxy and analytics ingest",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "openai": "^4.0.0"
  }
}
