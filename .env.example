# Optional AI & analytics features (disabled by default in the public build).
# Copy this file to .env.local and fill in your values — never commit secrets.
# See backend/README.md for instructions on deploying the backend.

VITE_API_BASE_URL=
VITE_ENABLE_AI=false
VITE_ENABLE_ANALYTICS=false
